/**
 * @author: zooey
 * Date: 18/07/09
 * desc: 插件-mark && alert && 其他
 * use: this.$mark.show({obj}) || this.$mark.show('string') && this.$mark.hide()
 */
import Mask from './src/mark.vue'

export default Mask
